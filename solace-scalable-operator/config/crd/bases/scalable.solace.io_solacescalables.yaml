---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: solacescalables.scalable.solace.io
spec:
  group: scalable.solace.io
  names:
    kind: SolaceScalable
    listKind: SolaceScalableList
    plural: solacescalables
    singular: solacescalable
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SolaceScalable is the Schema for the solacescalables API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              clusterUrl:
                type: string
              container:
                description: Foo is an example field of SolaceScalable. Edit solacescalable_types.go
                  to remove/update
                properties:
                  env:
                    description: PortsRange string `json:"portsRange,omitempty"`
                    items:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  image:
                    description: standard docker command docker run -d -p 8080:8080
                      -p 55555:55555 --shm-size=1g --env username_admin_globalaccesslevel=admin
                      --env username_admin_password=admin --name=solace --mount type=bind,source=/mnt/solace,destination=/var/lib/solace,ro=false
                      solace/solace-pubsub-standard
                    type: string
                  name:
                    type: string
                type: object
              haproxy:
                properties:
                  namespace:
                    type: string
                  serviceName:
                    type: string
                type: object
              pvClass:
                type: string
              replicas:
                format: int32
                type: integer
            type: object
          status:
            description: SolaceScalableStatus defines the observed state of SolaceScalable
            properties:
              solaceStatus:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
